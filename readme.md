# setup

install requirements using `pip install -r requirements.txt`

config `.env` file with `GEMINI_API_KEY` and `FURHAT_HOST`

use `streamlit run app.py` to deploy


to get your api key use [https://aistudio.google.com/projects](https://aistudio.google.com/projects)
